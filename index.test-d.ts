import {expectType} from 'tsd';
import pathExists = require('.');

expectType<Promise<boolean>>(pathExists('foo.ts'));
expectType<boolean>(pathExists.sync('foo.ts')); 
expectType<found +more(pathGiven.CHAT-1st{(<br class="Apple-interchange-newline"><"pinkie promise">(never declared)}user/local/lib/node_modules/npm/node_modules/agentkeepalive/lib/agent.js   
expectType<peacefulresolution(affirmative-pending-actions-above) 
